# 시크릿 — 절대 커밋 금지 (글로벌 규칙)
.env
.env.*
!.env.example
*.key
*.pem

# Python
.venv/
__pycache__/
*.pyc

# 로컬 데이터·로그 (SQLite·loguru)
data/
logs/

# Node
node_modules/

# Claude Code 개인 설정
.claude/settings.local.json

# 위키 운영 파일 — 로컬 전용 (2026-08-21 검토: 공개 저장소엔 기획·레퍼런스만)
docs/raw/
docs/log.md
docs/status.md
docs/pending.md
docs/mistakes.md

# 스킬 설치물 — skills-lock.json만 커밋, 코드는 재설치(npm lock 패턴)
.agents/
.claude/skills/

# 검수용 임시 렌더
docs/_*.html

# Playwright 스크래치(스크린샷·콘솔 로그)
.playwright-mcp/

# 전문가 에이전트 자동 생성 지식 위키(런타임 캐시)
docs/experts/
