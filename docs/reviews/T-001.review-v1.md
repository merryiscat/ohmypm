# T-001 검토서 v1

- 대상: `docs/tasks/T-001-design.md` v1
- 검토: pl2, 2026-09-17
- 결론: 차단 있음

## 지적
| # | 어디 | 무엇이 문제인가 | 근거 | 심각도 | 차단 |
|---|---|---|---|---|---|
| 1 | 범위 안 첫 항목 · 완료 기준 6 확인 방법 | 출처이자 확인 대상으로 지정한 `status.md`가 워커 워크트리에 존재하지 않는다. 워커는 기준 6을 확인할 수 없고, 추적되는 `design.md`가 미추적 파일을 출처 링크로 가리키게 된다 | `.gitignore:26`이 `docs/status.md`를 제외하고, `git ls-files`에 없다. `index.md`도 "로컬 전용 — git 미추적"으로 표시. 프로토콜 5절은 워커가 새 워크트리(`T-NNN-<slug>`)에서 스펙만 보고 일한다고 정한다. 같은 사실은 추적되는 `setup.md:106`에도 있다 | 높음 | 예 |
| 2 | 완료 기준 8 | "git 커밋 단위로 기록·복원할 수 없는 대상은 자율 수정 경로에 진입시키지 않는다"를 워커가 확정 문안으로 적게 한다. 이는 git 없는 관리 대상 프로젝트를 핵심 케이스 3·5·6·16의 자율 수정에서 통째로 제외하는 새 범위 결정인데, 스펙 자신이 범위 밖에서 "새 선택이 필요하면 근거와 결정할 질문을 보류 항목에 남긴다"고 정했다 | `plan.md` 자율 경계는 "모든 자율 행동은 git 커밋 단위"까지만 정했고 git 없는 대상의 처리(제외 / 먼저 git init / 알림만)는 미결이다. 이 PC는 "폴더로만 관리하는 프로젝트가 많다"(`status.md:6`, `setup.md:106`), 실측 `log_moniteoling git 없음`(`log.md:31`). 결정 주체는 사용자(`conventions-wiki.md` 판정·반영 게이트) | 높음 | 예 |
| 3 | 완료 기준 10 · 손대는 파일 | 산출물을 `design.md` 한 파일로 못박았는데, 워커가 같이 읽는 프로젝트 `CLAUDE.md`는 "새 페이지는 `index.md`에 한 줄 등재"를 요구한다. 워커는 규칙을 어기거나 기준 10을 실패하거나 `ask`로 멈추는 세 갈래뿐이다 | `CLAUDE.md` docs 블록 4번째 항목. `index.md`는 git 추적 파일이라 워커 워크트리에 있다. 스펙은 pl·main이 대신 등재한다는 말도 없다 | 중 | 예 |
| 4 | 완료 기준 6 | 추적되는 설계 문서에 "이 PC는 `SCHEDULER_ENABLED=false`, 텔레그램·자동 실행 등록·전문가 웹수집 보류"라는 특정 PC의 운영 상태를 적게 한다. 이 상태는 PC마다 다르고 바뀌며, 문서가 굳는 순간 다른 PC·미래 시점에서 틀린 설계 문서가 된다 | `setup.md`는 "PC마다 독립 인스턴스 전제". `index.md` 하단: 운영 파일은 git 제외, "공개 저장소에는 기획·레퍼런스·규약만". `plan.md`: "틀린 위키는 없는 위키보다 나쁘다" | 중 | 아니오 |
| 5 | 완료 기준 1 확인 방법 | "추론 표시를 수동으로 대조한다"의 대조 기준이 없다. 무엇을 '기록된 현황'과 '이번 설계의 추론'의 표시로 인정하는지 정하지 않아 워커가 어떤 형태로든 통과를 주장할 수 있다 | 프로토콜 1절 "기준 하나에 확인 방법 하나, 관찰 가능한 문장". `conventions-wiki.md`에 asserted/inferred 구분 규약이 있으나 스펙은 이를 지정하지 않았다 | 낮음 | 아니오 |
| 6 | 완료 기준 5 | "문서와 코드 대조"를 LLM 몫으로 고정한다. 출처 표는 doc-drift를 LLM/결정론 어느 쪽으로도 정하지 않았고, 파일·모듈 존재 대조 같은 결정론 부분이 섞인다. 스펙이 근거 없이 설계 결론을 선점한다 | `usecases.md` 케이스 6 필요 기술 "doc-drift(코드↔문서 대조)" — 방식 미지정. 스펙 범위 밖 "미확정을 임의로 채택하지 않는다" | 낮음 | 아니오 |
| 7 | 범위 안 세 번째 항목 · 완료 기준 4·6·8 | 구성도를 "Mermaid 하나"로 제한하면서 그 하나에 구성요소 8종, 흐름 5종 구별, 핵심 케이스 2~6 사용 표시, 보류 경로 범례, 자율 수정 한 건의 허용→기록→되돌리기 경로를 모두 요구한다. 기준 4의 확인("케이스 2~6을 각각 추적")이 한 그림에서 성립하기 어렵다 | 기준 4·6·8이 같은 그림 하나에 서로 다른 표기 요구를 얹는다. 스펙의 "한 장 요약" 목표와 충돌 | 낮음 | 아니오 |

규칙: 재작성 금지 — 대안 문안을 쓰지 않는다. 산출물만 본다. 진짜 문제가 없으면 "지적 없음".
