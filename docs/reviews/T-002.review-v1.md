# T-002 검토서 v1

- 대상: `docs/tasks/T-002-project-setup.md` v1
- 검토: pl2, 2026-09-17
- 결론: 차단 있음

## 지적
| # | 어디 | 무엇이 문제인가 | 근거 | 심각도 | 차단 |
|---|---|---|---|---|---|
| 1 | 완료 기준 1 · 범위 안 | 조사의 출발 입력인 "현재 설정된 관리 루트"와 "기존 등록 목록"이 어디에 있는지 스펙이 말하지 않고, 둘 다 워커 워크트리에 없다. 관리 루트는 `.env`의 `PROJECTS_ROOT`, 등록 목록은 `data/ohmypm.db`인데 둘 다 gitignore라 새 워크트리(`new-top-level`)에는 없고 main 체크아웃(`D:/dev/project/ohmypm`)에만 있다. 게다가 "루트를 확인할 수 없으면 누락 범위를 적는다"는 도피 조항 때문에 워커가 빈 대상표로 기준 1을 통과시킬 수 있고, 그러면 기준 2~7이 전부 공허해진다. T-001 검토 지적 1(미추적 출처 의존)과 같은 유형의 재발이다 | `.gitignore:2` `.env`, `:14` `data/`; `git ls-files`에 둘 다 없음. `.env.example`: "PROJECTS_ROOT= … PC마다 다르다. 필수". `git worktree list`: main 체크아웃과 pl만 있고 워커는 별도 경로에 뜬다(T-001 실측). 프로토콜 4절 "워커 워크트리는 저장소 기본 base에서 뜬다" | 높음 | 예 |
| 2 | 손대는 파일 · 완료 기준 4·7 | 추적되는 `docs/setup-rollout.md`에 이 PC의 프로젝트 목록·지침 차이·환경변수 이름·포트·외부 연동을 싣는다. 절대경로·비밀값은 뺐지만 인벤토리 자체가 PC 전용 정보이고, 저장소는 공개 GitHub다. 한 번 푸시되면 지워도 이력에 남는다 | `index.md` 하단 "공개 저장소에는 기획·레퍼런스·규약만 올린다". `setup.md` 전제표: PC 전용 정보는 gitignore 칸. `setup.md §1` 원격은 `github.com/merryiscat/ohmypm`. `deliverables.md` 구축 절: 되돌릴 수 없는 사고는 별도 점검 항목 | 중 | 아니오 |
| 3 | 완료 기준 2·4 · 필요 도구 | 워커가 관리 루트 아래 모든 프로젝트의 지침·스킬·MCP·훅 설정과 "필요한 환경변수 이름"을 읽게 하면서, 읽지 말아야 할 파일(`.env`, 자격증명이 든 MCP 설정 등)을 정하지 않았다. "비밀값을 출력하지 않는다"는 출력만 막지 읽기는 막지 않아, 다른 프로젝트의 토큰이 워커 문맥과 보고에 섞일 수 있다 | 기준 4 "값은 기존 문서·선언 파일에서" — `.env`도 선언 파일로 읽힌다. `.env.example` 주석 "`.env`는 커밋 금지"가 비밀값 저장처임을 보여줌. `status.md` 다음 항목에 "obsidian 키 폐기·재발급"이 있어 이 PC 프로젝트에 노출된 키가 실재한다 | 중 | 아니오 |
| 4 | 완료 기준 5 · 범위 밖 | 워커에게 프로젝트별 "정확한 수정 파일·변경안·설치 도구 이름·출처·버전·순서·확인·원복"까지 쓰게 한다. 이는 범위 밖이 "후속 스펙으로 진행"한다고 한 그 스펙의 본문이며, 프로토콜상 스펙 작성은 pl 몫이다. 워커(구현자)가 다른 프로젝트 지침의 변경안을 설계하게 되어 조사 문서와 스펙의 경계가 사라진다 | 스펙 범위 밖 "실제 적용은 조사 결과를 담은 프로젝트별 후속 스펙으로 진행". `protocol.md §1` 스펙은 pl이 쓴다. `roles.md` 구현 워커 경로 = "스펙의 '손대는 파일'" | 중 | 아니오 |
| 5 | 완료 기준 3 | "작업 구조 필요성은 규모·단계로 판단한다"에 판단 기준(어떤 규모·어느 단계부터)이 없다. 확인 방법 "deliverables의 분류·현재 역할/절차와 대조"도 통과/실패를 가르는 규칙을 주지 않아 워커의 임의 판단을 pl2가 판정할 수 없다 | `plan.md` 작업 구조 절: "ohmyPM은 운영 중·케이스 18이라 깐다. 작은 프로젝트는 안 깐다" — 문턱 없음. `deliverables.md`는 단계 분류만 있고 작업 구조 설치 기준은 없음. 프로토콜 1절 "관찰 가능한 문장" | 낮음 | 아니오 |
| 6 | 완료 기준 1 · 필요 도구 | "기존 등록 목록"은 SQLite DB인데, 필요 도구는 "파일·폴더 조회와 git의 읽기 기능만"이고 "앱 코드 실행 금지"다. DB를 읽으려면 `sqlite3`·python 질의 또는 떠 있는 대시보드 API가 필요하고, 어느 쪽이 허용인지 스펙이 정하지 않았다 | `setup.md §6` "`data/ohmypm.db`는 첫 기동에 생성", 등록 목록은 `POST /api/scan` 결과. 스펙 범위 밖 "서버 기동" 금지, 필요 도구 "앱 코드를 실행하지 않는다" | 낮음 | 아니오 |

규칙: 재작성 금지 — 대안 문안을 쓰지 않는다. 산출물만 본다. 진짜 문제가 없으면 "지적 없음".
