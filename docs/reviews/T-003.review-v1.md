# T-003 검토서 v1

- 대상: `docs/tasks/T-003-structure-rollout.md` v1 (차선 L)
- 검토: pl2, 2026-09-18
- 결론: 차단 있음

## 지적
| # | 어디 | 무엇이 문제인가 | 근거 | 심각도 | 차단 |
|---|---|---|---|---|---|
| 1 | 범위 안 두 번째 항목(13행) · 등급 근거(4행) | 커밋되는 스펙 본문에 이 PC 고유 정보가 실린다 — 사용자 홈 경로(`C:\Users\minhy\project`), 대상 프로젝트 이름 23개 전부, 관리 대상 개수(25·23). `docs/tasks/`는 git 추적 파일이고 원격은 공개 GitHub다. 한 번 푸시되면 지워도 이력에 남는다 | `git remote -v` = `github.com/merryiscat/ohmypm`. `index.md` 하단 "공개 저장소에는 기획·레퍼런스·규약만". protocol 6절(0.3.3) 항체: 커밋 파일에 "PC 경로·사용자명·프로젝트 이름·개수"를 옮겨 적지 않는다. 바로 앞 커밋 dbaf4e2·206e436이 T-001 검토서·T-002 검증 표에서 같은 종류 정보를 지웠다. T-002 검토서 #2가 같은 지적이었고 사용자 결정으로 로컬 전용(`.gitignore:45`, `.worktreeinclude:8`)이 됐다. 옮길 자리는 이미 있다 — 로컬 전용 `docs/setup-rollout.md`가 T-002 산출물로 이 PC의 대상표를 갖고 있고, 그 파일은 `.worktreeinclude`로 새 워크트리(워커 포함)에 복사된다. 스펙이 허용 목록을 그 파일(또는 gitignore된 별도 입력 파일·실행 인자)로 가리키면 본문에는 개수·이름이 남지 않는다 | 높음 | 예 |
| 2 | 완료 기준 7·8 · 손대는 파일 세 번째 항목 | 결과 보고서 `docs/rollouts/T-003-structure-rollout.md`가 커밋 파일인데 "프로젝트·경로·…·커밋 SHA" 23행 결과 표, 실행 명령, `ohmyPM`·`kickoff_pack` 제외 사유를 담게 한다. #1과 같은 유출인데 양이 더 많다(절대경로 23개 + 대상별 SHA). `docs/rollouts/`는 `.gitignore`·`.worktreeinclude` 어디에도 없어 기본이 추적이다. 덧붙여 새 페이지를 만들면 CLAUDE.md docs 블록 "새 페이지는 `index.md`에 한 줄 등재"와 만나는데 스펙은 index 처리(등재 여부·로컬 전용 표시)를 정하지 않았다(T-001 검토서 #3과 같은 구멍) | `.gitignore` 전문에 `docs/rollouts` 없음. protocol 6절 항체 원문은 "검증 표"를 말하지만 근거는 "커밋되는 파일이다"이고 결과 보고서도 같은 성질이다. T-002는 기준 7에서 산출물을 "로컬 전용·gitignore 대상·git 미추적"으로 명시하고 "설정 누락은 실패"로 못박아 이 문제를 풀었다(선례). 기준 8의 확인 방법이 그 파일과 대상별 커밋의 대조라서, 파일이 로컬 전용이면 검증자가 그 내용을 검증 표에 옮겨 적지 않는 조건(6절)도 함께 걸린다 | 높음 | 예 |
| 3 | 완료 기준 3·5 | 두 규칙이 재사용하는 스크립트의 설계와 어긋난다. 기준 5는 "기존 블록 때문에 교체가 필요하면 보존 충돌로 건너뛴다"인데 `ws-upgrade.sh`는 기존 블록을 찾아 **교체**하는 것이 정상 동작이다. 기준 3은 "버전이 같으면 바꾸지 않는다"만 정해 **버전이 다른 기존 설치**(예: 0.3.2가 이미 깔린 프로젝트)를 어떻게 하는지 비어 있다 — 기준 5대로면 그 프로젝트는 전부 건너뛰어지고 업데이트가 영원히 안 된다. 또 스크립트는 사전 검사 없이 바로 덮어쓰므로 "쓰기 전에 건너뛴다"를 지키려면 워커가 스크립트 밖에서 판정해야 하는데, 무엇을 '충돌'로 보는지(블록 유무·버전 차이·본문 변형) 스펙이 정하지 않아 어느 쪽으로 해도 통과를 주장할 수 있다 | `ws-upgrade.sh` 26~43행(`pat.sub` 교체), 20~23행(검사 없이 덮어씀). SKILL.md "업데이트" 절: 블록을 "갈아 끼우고 diff를 보여 준다". protocol 1절 "기준 하나에 확인 방법 하나, 관찰 가능한 문장" | 중 | 예 |
| 4 | 완료 기준 3·5·6 · 손대는 파일 두 번째 항목 | `ws-upgrade.sh` 24행이 대상의 `core.hooksPath`를 무조건 `.githooks`로 바꾼다. 대상에 이미 훅 경로가 설정된 프로젝트(Node의 husky 등)가 있으면 기존 훅이 전부 꺼진다. 기준 5의 보존 규칙은 파일만 다루고 Git 설정은 없으며, 기준 3의 사전 검사 항목에도 없다. 그리고 기준 6 "프로젝트 훅은 실행하지 않는다"는 이 설정 직후 커밋하면 방금 설치한 pre-commit(과 대상 기존 훅)이 돌게 되는 것과 부딪히는데, 회피 수단을 스펙이 정하지 않았다. 대상에 Node 프로젝트가 포함되는 것은 기준 4가 전제한다(대상 저장소를 열어 확인하지는 않았다 — 검사 항목이 없다는 점이 문제) | `ws-upgrade.sh:24`. 기준 3 검사 목록: "Git 저장소 루트 여부, staged/unstaged/untracked 변경, 설치 버전" — 훅 경로 없음. 되돌리기 어려운 변경 23곳은 이 스펙이 스스로 L 사유로 든 것 | 중 | 예 |
| 5 | 목표(8행) | 플러그인 버전을 `0.3.2`로 적었는데 저장소의 `plugin.json`은 `0.3.3`이다. 스크립트가 도장을 `plugin.json`에서 읽어 동작은 0.3.3으로 되지만, 스펙은 검증의 기준 문서라 검증자가 0.3.2를 기대하면 기준 3·6 판정이 어긋난다 | `plugin/.claude-plugin/plugin.json` `"version": "0.3.3"`(커밋 dbaf4e2, 2026-09-18 06:01). 이 PC의 `docs/protocol.md` 첫 줄도 v0.3.3 | 낮음 | 아니오 |
| 6 | 완료 기준 1 | "플러그인 설치 완료 절차가 rollout을 이어서 호출"의 진입점이 무엇인지 없다. 이 플러그인에는 설치 후 자동 실행 장치가 없고(`plugin/`에 hooks 없음, `plugin.json`에 hooks 항목 없음), 설치는 SKILL.md를 사용자 발화로 발동해 손으로 한다. 확인 방법 "설치 완료 진입점에서 … 통합 실행 기록"의 시작점이 불명이라 워커가 어디서 기록을 시작해도 된다 | `ls plugin/.claude-plugin` = `plugin.json`만. SKILL.md 2절·업데이트 절은 사람이 부르는 절차 | 낮음 | 아니오 |
| 7 | 완료 기준 2 | 탐색 조건 `has_wiki=1 AND enabled=1`인데 `GET /api/projects`는 `enabled=1`만 거르고 `has_wiki`는 응답 칸으로만 준다(클라이언트가 걸러야 한다). API 주소·포트도 없다 — T-002는 `http://127.0.0.1:8123/api/projects`로 명시했다. 워커가 할 수 있는 일이지만 확인 방법이 "임시 입력의 대상 목록"이라 서버 주소가 스펙에 없으면 실제 API 경로가 검증되지 않는다 | `src/web/routers/api.py:16~24`(`list_projects()` 기본 `enabled_only=True`, `SELECT *`). T-002 스펙 범위 안 항목 | 낮음 | 아니오 |

규칙: 재작성 금지 — 대안 문안을 쓰지 않는다. 산출물만 본다. 진짜 문제가 없으면 "지적 없음".
