# T-004 검토서 v1

- 대상: `docs/tasks/T-004-daily-report-noise.md` v1 (차선 M — 차단 항목만)
- 검토: pl2, 2026-09-18
- 결론: 차단 있음

## 지적
| # | 어디 | 무엇이 문제인가 | 근거 | 심각도 | 차단 |
|---|---|---|---|---|---|
| 1 | 완료 기준 5 · 목표 | 일간보고가 스스로 만드는 완료 카드가 다음 날 "24시간 내 새 이슈"로 잡혀 점검이 되먹임한다. 기준 5는 스캔 재생성 행만 다루고 이 출처를 빠뜨렸다 — 기준 1~5를 전부 통과해도 오늘 점검된 프로젝트는 내일 또 점검되어 목표("손대지 않은 프로젝트 점검 잡음 제거")가 남는다 | `daily_report.py:104~108` `_has_activity`는 `created_at >= cutoff`만 보고 `source`를 거르지 않는다. `_apply_updates`(221행)가 PM의 `{"done"}`마다 `issues.add_done`을 부르고, `issues.py:99~102`는 `source='daily_report'`, `created_at=datetime('now','localtime')`로 넣는다. 03:00 배치에서 만든 카드는 다음 날 03:00 cutoff 안에 있다. 실측: 2026-09-18 03:18 생성된 `kind=done` 행(사용자 제시) | 높음 | 예 |
| 2 | 완료 기준 5 · 손대는 파일 · 범위 밖 | "같은 스캔 회차에 stale 전환이 있으면", "회차를 입증 못 하는 과거 행"을 판정하라는데 회차와 stale 전환 시각을 담는 자료가 없다. 범위 밖이 스키마 마이그레이션을 금지하므로 워커는 기준을 못 채우거나 임의 근사(생성 시각 묶기 등)로 통과를 주장하게 된다 | `src/db/schema.sql:13~28` issues에 회차·stale 시각 칸 없음. `src/scan/__init__.py`에 회차 식별자 없음. `issues.py:65~83` `mark_stale`은 status만 바꾸고 시각을 남기지 않는다. 손대는 파일의 "기존 저장 수단에 활동 구분을 남겨"는 어느 칸에 무엇을 쓰는지 정하지 않아 관찰 가능한 기준이 아니다(protocol 1절) | 중 | 예 |

규칙: 재작성 금지 — 대안 문안을 쓰지 않는다. 산출물만 본다. 진짜 문제가 없으면 "지적 없음".
