# ohmyPM 기획 (킥오프 1부 초안)

derived_from: 대화 2026-08-20 (킥오프 1부 인터뷰)

## 1부 요약

ohmyPM은 사용자의 모든 로컬 프로젝트를 매일 돌보는 **메타 PM 에이전트**다. 킥오프팩을
만들면서 확인한 문제 — 하네스 엔지니어링과 프로젝트 환경 관리가 부실하면 모델 품질이 확
떨어진다 — 를 해결하기 위해, 프로젝트 매니저가 할 일을 에이전트가 매일 대신 수행한다.
형태는 **Claude Code 기반 에이전트**(스킬 + 스케줄, 별도 앱·화면 없음)이고, MVP 범위는
① 환경·하네스 점검(CLAUDE.md·스킬·훅·설정의 부패/노후 감지·업데이트), ② 진행 상황
추적 + 데일리 브리핑(오늘 할 일 제안·우선순위·일정 제시), ③ 문서·위키 건강 관리(llmwiki
lint 연계), ④ 자율 업무 실행(사용자 판단이 필요 없는 업무는 직접 처리)이다. 보고 채널은
**텔레그램 알림**. 산출물은 코드/하네스(스킬·스케줄 구성)다.

## 핵심 골자 — 기반·진화·뼈대 (derived_from: 대화 2026-08-22)

세 층으로 본다:
- **킥오프팩** = 프로젝트의 **기반을 올바르게 잡는다** (기초 결정·스택·하네스를 초기에 바로 세움)
- **ohmyPM** = 그 기반 위에서 **올바르게 세워나가며 변화하는 환경에 대응한다** (매일 돌보기)
- **위키(llmwiki)** = 그 둘을 잇는 **연속성의 뼈대** — 기반의 결정·전제를 살아있게 이어가고, 환경이 바뀌어 전제가 낡으면 그걸 들춰낸다

함의(2026-08-22 근본 검토 결론): 위키가 PM을 개선하는 지점은 **능동적·강제적 장치**(때 되면 안건을 밀어올리는 재부상 훅, 항체를 만들게 강제하는 규칙, 흡수된 걸 지우는 다이어트)이지 **수동 보관함**(검색·index·페이지 택소노미·raw 축적)이 아니다. 틀린 위키는 없는 위키보다 나쁘다(신뢰되므로). 그래서 위키 개선의 방향은 검색을 붙이는 게 아니라 **강제 장치 강화 + 보관함 ceremony 감축**이다. 규모·리스크가 큰 프로젝트일수록(예: naverblog 자율 운영) 밥값이 크고, 작고 미완인 프로젝트(현재 ohmyPM 자신)에선 관리 비용이 이득을 넘볼 수 있다.

## 스택·하네스 (2·3부 확정, 2026-08-25)

**유즈케이스**: [docs/usecases.md](usecases.md) 18케이스 (핵심 2~6: 대시보드·형식표준화·이슈상담·반복실수·SE문서). 케이스 1 벤치마크는 별도 프로젝트로 분리.

**스택**: Claude Code 코어 + Python(uv) · 텔레그램 봇 · **웹 대시보드(FastAPI+HTML)** · SQLite(로컬) · **로컬 실행 스케줄**(cron/scheduled + heartbeat) · git · lychee
- 1부 "화면 없음" → **웹 대시보드 추가**(케이스 2·15)
- **실행 = 로컬 확정**: 로컬 프로젝트 파일 접근 필수라 클라우드 Routines 배제(references Zenn 결론)
- 상태 저장 = SQLite(사용자 결정 — 로컬 도구라 설치 0·서버 없음)
- **역추적 결과 주의(2026-09-14, [usecases 합산규칙 3](usecases.md))**: 이 줄의 항목 중 **heartbeat는 현재 코드에 없다**(빈 5분 잡을 09-09에 제거, 30cb85c — 케이스 4 착수 시 재부착), **lychee는 코드 0줄 + 케이스 9 하나에만 걸린다**(범위 결정 대기). 나머지 6개는 핵심 케이스에 닿는다

**하네스**(로컬 `.claude`): fastapi 스킬 신규 설치 / screen-plan·web-design-guidelines·ponytail·grill 글로벌 재사용 / Playwright·context7 MCP(승인 대기). supabase·텔레그램봇 스킬은 스택이 요구 안 해 미설치.

## 작업 구조 1.0.0 (2026-09-21 사용자 확정)

derived_from: 대화 2026-09-21. 기존 요청→설계→구현→검증 흐름을 main/pl/work로 재구성한다.

- **main**: 요청 분류·단순 수정·사용자 알림·환경 준비와 회수·검증된 결과의 로컬 머지.
- **pl**: 사용자와 기획·설계·추론, 완료 기준·검증 방법·특화 하네스 합의, 코드·실제 동작 검증과 품질 판정.
- **work**: 승인된 버전의 구현·검증을 독립 워크트리·브랜치·세션에서 수행한다. 독립성이 확인된 작업만 최대 두 개 병렬이다.
- pl의 질문은 main이 대화 위치를 안내하고 사용자와 pl이 직접 논의한다. 특정 버전의 구현 요청이 있어야 시작한다.
- 공유 문서·영속 상태와 Orca 알림을 함께 쓴다. 판정은 설계 버전·결과 커밋·main 기준 커밋에 묶고, 변경되면 재검증한다.
- 작업 환경은 스킬·MCP·의존성·설정·포트·데이터를 필요한 만큼 조합한다. Docker 필수나 OS 보안 경계를 뜻하지 않는다.
- 기존 PM 점검·보고 기능은 보존한다. ohmypm에 먼저 적용하고 단일 모델과 반복 비교한 뒤 사용자가 확대를 결정한다.

역할은 [roles.md](roles.md), 모델은 [workflow.json](workflow.json), 절차는 [protocol.md](protocol.md),
실행·복구는 [workflow-guide.md](workflow-guide.md), 완료 기준은 [T-005](tasks/T-005-v1-workflow.md)가 소유한다.
상시 pl2·pl3, pl의 문서 전용·두 번 호출 제한, main만 구현하던 0.x 규칙을 대체한다.

## 미확정 질문

- **성공 기준** — ✅ **확정(grill 2026-08-26)**: MVP 성공 = 매일 자동 실행돼 관리 대상의 **놓친 미해결·기한·방치·형식붕괴를 감지·알리고, 사용자가 "안 봐도 안 놓친다"를 신뢰하는 상태**. petieclark 원칙(실패를 시끄럽게 정의 / 수동 2주 전엔 자동화 금지 → 처음엔 감지·알림·초안, 신뢰 쌓이면 자율 확대). 정량 SLA(대응시간·방치 수 등)는 데이터 쌓인 뒤 부착
- **관리 대상 목록** — `C:\Users\minhy\project` 하위 전부인가, 지정 목록인가? → **케이스 12(사용자가 등록/제외)로 해소 방향**. 실측 40폴더 중 docs 위키 ~18개
- **실행 스케줄** — ✅ **로컬 확정(2·3부)**: cron/scheduled tasks + heartbeat. 클라우드 Routines 배제(로컬 파일 접근 불가). 매일 몇 시·heartbeat 주기는 구현 시 결정
- **자율 업무 실행의 경계** — ✅ **확정(grill 2026-08-26)**: 사전 분리 + **화이트리스트**. 자율 허용 행동을 **사용자가 명시 등록한 목록으로만** — 에이전트는 "안전한가" 자가판단 금지, "목록에 있나"만 확인(결정론적 = confused-deputy 방지, ScopeGate 패턴). 목록 추가는 사용자 승인.
  - **자율 폭은 넓게 등록 가능**(코드 손봄·케이스 3·5·6·16 포함) — **단 판단 주체는 항상 사용자**. 코드 수정도 유형별 등록(예: 테스트·타입·포맷 자율 / 로직 변경 상담)
  - **안전장치(불변)**: ① 모든 자율 행동은 git 커밋 단위 → 케이스 15(자율내역·롤백) ② **되돌리기 불가능한 것**(삭제·외부 발신·DB 마이그레이션·force push)은 **화이트리스트 원천 제외** — 아무리 넓혀도 못 넣음
- **코드리뷰 범위** — ✅ **확정(grill 2026-08-26)**: 코드 손봄까지 하되 **화이트리스트에 등록된 수정 유형만** 자율(로직 변경 등은 상담). 코드리뷰 '소견'은 **케이스 4(이슈 상담)로 흡수** — "이 커밋 위험해 보여요, 어떻게 할까요". 리뷰 엔진 재구현 아님(`/code-review`가 함) — '언제 리뷰가 필요한지 감지 + 소견 제시'가 ohmyPM 몫
