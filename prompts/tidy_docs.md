프로젝트 '${project_name}'(${project_path})의 **작업 기록을 실제와 맞추는** 일이다.
곧 일간보고가 시작되는데, 기록이 실제 작업과 어긋나 있으면 총괄이 틀린 전제로 보고를 받는다.
(실제 사례: 이미 끝낸 일이 기록에 '착수 전'으로 남아 있어 보고가 어긋났다.)

[실제로 있었던 일 — 코드가 모아온 사실]
${facts}

[할 일]
`docs/status.md`·`docs/log.md`·`docs/pending.md`를 읽고, 위 사실과 어긋난 곳만 고쳐라.

1. **끝난 일이 '진행 중'·'미해결'에 남아 있으면** 완료로 옮긴다. 근거(커밋 요약·파일명)를 한 줄로 남긴다.
2. **실제로 한 일이 기록에 없으면** `log.md` 맨 위에 `## [YYYY-MM-DD] <작업> | <제목>` 한 줄을 추가한다.
3. **`pending.md`의 보류 안건 중 재검토 조건이 이미 충족된 것**이 있으면 그 행에 충족 사실을 덧붙인다(행을 지우지는 않는다).
4. 지금 작업 중인 파일이 있으면 `status.md`의 진행 중에 그 갈래가 보이게 한다.

[반드시 지킬 것]
- **근거 없는 추측 금지.** 위 사실이나 문서에서 확인되는 것만 쓴다. 확인이 안 되면 그대로 둔다 — 비워두는 편이 틀리게 채우는 것보다 낫다.
- 사람이 사용자에게 확인해야 할 일(외부 화면에서 눌러야 하는 설정 등)은 **끝났다고 단정하지 말고** "확인 필요"로 남긴다.
- `docs/` 안의 **기존 문서만** 고친다. 새 문서를 만들거나 문서를 삭제하지 않는다.
- 코드는 건드리지 않는다. 기록만 고친다.
- 고칠 게 없으면 아무 파일도 바꾸지 말고 그렇게 답하라. 억지로 고치지 않는다.

[출력]
문서 수정을 마친 뒤, 마지막 줄에 JSON 하나만 덧붙여라.
{"changed": ["고친 파일 경로"], "summary": "무엇을 실제와 맞췄는지 한 줄", "needs_user": "사용자 확인이 필요한 것 한 줄(없으면 빈 문자열)"}
