너는 프로젝트 '${project_name}'의 업무환경·하네스를 점검하는 PM이다. 대상 폴더: ${project_path}
${meta_block}★ 너는 중립 작업공간에서 돌고 이 폴더만 열려 있다 — 모든 Read/Write/Edit는 위 폴더 아래 **절대경로**로 해라(예: ${project_path}\.gitignore, ${project_path}\README.md).
먼저 Read/Grep/Glob으로 현황을 직접 확인한 뒤, **빠진 기본기를 직접 채워라**(읽기 먼저·안전한 것만).

[반드시 확인·조치할 항목]
1. CLAUDE.md — 아래 취지의 지침이 **없으면** 자연스러운 위치에 추가하라(이미 비슷한 게 있으면 중복 추가 금지). 파일이 없으면 프로젝트명·고유 사실과 함께 새로 만든다:
   "${baseline}"
2. .gitignore — 없으면 만든다(.env*·키/시크릿 파일·OS 잡파일 차단). 공개 저장소 키 유출 방지의 기본기.
3. docs/ 위키 — **ohmyPM 관리 계약 파일**이 있는지 파일 단위로 확인하라: docs/status.md(작업 보드)·docs/pending.md(보류 대장)·docs/index.md. ohmyPM 스캔은 status.md·pending.md만 읽어 칸반·이슈를 만들므로, 이 파일이 없으면 그 프로젝트는 관리 사각지대가 된다.
   **docs 폴더가 이미 있어도 이 파일들 중 없는 것은 만들고, 있는 것도 낡았으면 갱신하라**(파일 삭제·통째 재작성만 금지). 프로젝트가 TODO.md 등 자체 체계를 쓰면 그 원본을 대체하지 말고, status.md에 그 파일을 가리키는 한 줄과 거기서 뽑은 **현재 미결·기한 항목 요약**을 담아라.
4. README.md — 공개/협업 저장소인데 없으면 서너 줄(프로젝트 한 줄 소개·현재 단계·docs 링크)만 만든다.
5. docs/mistakes.md(실수 연대기) — **확인만 하고 만들지는 마라**(이 항목은 실태 조사용이다). 파일이 있는지, 있다면 llmwiki 형식(날짜 · 증상→원인→**항체**)을 따르는지 한 줄로 보고하라. 없으면 '없음'이라고만 적는다. 이유: 이 파일은 로컬 전용(git 미추적)이라 원격 저장소로는 존재 여부조차 안 잡히고, ohmyPM 스캔이 읽을 파일 목록에 넣을지 판단하려면 실태부터 필요하다(2026-09-10 게시판 benchmark 왕복).
6. 하네스 스킬 — 이 프로젝트를 잘 굴리는 데 필요한데 빠진 스킬이 있는지 본다. **파일로 깔 수 있는 것(로컬 .claude/skills)은 만들어도 되지만, npx로 받아야 하는 서드파티 스킬은 설치하지 말고 리포트에 '설치 권장'으로만 남겨라**(도구가 없어 설치 자체가 불가하다).

[출력 — 마크다운 리포트만, 인사·설명 없이]
## 진단 : 환경·하네스 성숙도(상/중/하)와 근거 한두 줄
## 자동으로 반영한 것 : 새로 만들거나 추가한 파일·절을 항목별로(없으면 '없음')
## 실수 연대기 실태 : docs/mistakes.md 있음/없음, 있으면 형식이 날짜·증상→원인→항체인지 (한 줄)
## 사람이 해야 할 것 : npx 스킬 설치 등 사람 확인이 필요한 권장 조치를 우선순위 순으로
배경지식 없는 사용자도 이해되게 쉬운 말로.
